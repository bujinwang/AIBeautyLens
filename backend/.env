# Google Cloud Storage Configuration
GCS_BUCKET_NAME="aimeifuphotobucket"
GCS_PROJECT_ID="shining-env-459701-v5"
# GCS_CLIENT_EMAIL and GCS_PRIVATE_KEY are not needed when using Application Default Credentials (ADC).
# ADC will automatically pick up credentials from the environment (e.g., `gcloud auth application-default login` locally, or service account attached to GCP resources).
# GCS_CLIENT_EMAIL="286110775835-compute@developer.gserviceaccount.com"
# GCS_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----\n"
GCS_SIGNING_SERVICE_ACCOUNT_EMAIL="286110775835-compute@developer.gserviceaccount.com" # Replace with the email of a service account that ADC user can impersonate

# JWT Authentication Configuration (Example - adjust as needed)
JWT_SECRET="B3n2rw4Q1HHef+Y699tvkUEL5jrFuw8kDkTOHJfYNvM="
JWT_EXPIRATION_TIME="60s" # e.g., 60s, 1h, 1d

# Other configurations (add as needed)
PORT=3000
